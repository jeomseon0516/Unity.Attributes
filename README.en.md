# Jeomseon Unity Attributes

Lightweight, reusable inspector attribute declarations for Unity projects.

Custom drawers and method metadata UI are provided by `com.jeomseon.unity.editor-toolkit`.
The localization-specific `LocalizedStringAttribute` belongs to `com.jeomseon.unity.localization`.
